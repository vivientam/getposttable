<h1>I need a new post.
</h1>

<%= form_for @post do |f| %>

	<%= flash[:message] %>
	
	<div>
		<%= f.label :title %>
		<%= f.text_field :title %>

		<%= f.label :url %>
		<%= f.text_field :url %>

		<%= f.submit "Submit" %>
	</div>

<% end %>